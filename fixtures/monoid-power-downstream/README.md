# Monoid power downstream fixture

This fixture proves two passthrough theorems using only hash-pinned certificate
imports for `Mathlib.Algebra.Monoid.Power` and its `npa-std` dependencies.
No `npa-mathlib` source file participates in downstream proof acceptance.
